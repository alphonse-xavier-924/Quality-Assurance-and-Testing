// import { test } from "./fixture"; // Adjust the path as necessary

// test("continue after navigating to Tops", async ({ page }) => {
//   const selectElement = page.locator(
//     '//*[@id="maincontent"]/div[3]/div[1]/div[2]/div[4]//*[@id="sorter"]'
//   );

//   await selectElement.click();
//   await selectElement.selectOption({ label: "Product Name" });
//   //await page.waitForTimeout(5000); // 5000ms = 5 seconds
// });
